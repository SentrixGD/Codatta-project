# Codatta-project
Food classification and regression dataset project

execution order:

python -m src.materialization
python -m src.target_preprocessing
python -m src.data_analysis
python -m src.data_split
python -m src.data_normalization
python -m src.training_setup
